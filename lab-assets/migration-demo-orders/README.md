# Migration Demo Orders

This repository is a deliberately small but realistic migration fixture.

It contains source code, tests, CI, dependency metadata, ownership rules, security guidance, operational notes, and release history so that the migration lab exercises more than Git objects.

## Local development

```text
npm ci
npm test
npm run lint
npm run build
```

The project contains no real credentials. Values beginning with `EXAMPLE_` are safe placeholders.
