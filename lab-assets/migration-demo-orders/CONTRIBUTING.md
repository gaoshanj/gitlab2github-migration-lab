# Contributing

1. Create a feature branch from `develop`.
2. Run `npm ci`, `npm run lint`, and `npm test`.
3. Open a draft PR early when review is useful.
4. Keep commits focused and use a conventional commit prefix.
5. Do not commit credentials, personal data, or generated build output.
